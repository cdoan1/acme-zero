apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bootstrap-workspace-pvc
  namespace: bootstrap
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gp3-csi  # AWS EBS GP3 storage class for OpenShift