apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

namespace: bootstrap

resources:
  - namespace.yaml
  - sa.yaml
  - rbac.yaml
  - pvc.yaml
  - bootstrap-pipeline.yaml
  - pipeline-run.yaml
