apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

namespace: bootstrap

annotations:
  cluster: acme
  version: "v0.0.1"

resources:
  - namespace.yaml
  - rbac.yaml
  - pvc.yaml
  - bootstrap-pipeline.yaml