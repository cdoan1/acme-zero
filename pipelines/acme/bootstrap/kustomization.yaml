apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bootstrap

commonAnnotations:
  cluster: acme
  version: "v0.0.1"

resources:
  - bootstrap.pipelinerun.yaml

components:
  - ../../../bases/pipelines/bootstrap
